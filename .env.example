
# This file was auto-generated by src/lib/server/utils/env-utils.ts
# Feel free to comment and to replace default example values

ENVIRONMENT=dev # Required (dev|staging|prod) - The current environment (development, staging, production)

# Site Configuration
SITE_URL=localhost # Required - The base URL of your website. Used as an allow-list for redirects and for constructing URLs used in emails.
PROTOCOL=http # Required - The protocol to use for the SITE_URL (http or https)
PORT=3000 # Required (number) - The port to run the server on
APP_NAME=Sveltekit Template # Required - Application name to use as sender name in emails

# Supabase Configuration
SB_PUBLIC_URL=http://localhost:54321 # Required - Public Supabase URL
SB_PUBLIC_KEY=some_sb_public_key # Required - Public Supabase API Key
SB_PROJECT_URL=http://localhost:54322 # Required - Supabase Project URL for testing server-side Supabase operations
SB_STUDIO_URL=http://localhost:54322 # Required - Supabase Studio URL
SB_MAILPIT_URL=http://localhost:54323 # Required - Mailpit URL for testing email sending
SB_MCP_URL=http://localhost:54324 # Required - Supabase MCP URL for testing file uploads
SB_DB_URL=http://localhost:54325 # Required - Supabase Database URL for testing database connection
SB_SECRET_KEY=some_sb_secret_key # Required - Supabase Secret API Key for testing server-side Supabase operations
SB_STORAGE_ACCESS_KEY=some_sb_storage_access_key # Required - Supabase Storage Access Key for testing file uploads
SB_STORAGE_SECRET_KEY=some_sb_storage_secret_key # Required - Supabase Storage Secret Key for testing file uploads
SB_REGION=local # Required - Supabase Region for testing file uploads
SB_SCHEMA=sveltekit_template # Required - Supabase Schema for testing database connection
SB_POSTGRES_PASSWORD=postgres # Required - Supabase Postgres Password for testing database connection
SB_STORAGE_ENDPOINT=/storage/v1/s3 # Required - Supabase Storage Endpoint for testing file uploads

# SMTP Email Server Configuration
SMTP_HOST=smtp.gmail.com # Required - SMTP host for sending emails
SMTP_USER=some_smtp_user # Required - SMTP user for sending emails
SMTP_PASS=some_smtp_pass # Required - SMTP password for sending emails
SMTP_ADMIN_EMAIL=some_smtp_admin_email # Required - Admin email to receive notifications

# Oauth2 Configuration
OAUTH2_PROVIDER=google # Required - OAuth2 provider for testing authentication
OAUTH2_CLIENT_ID=some_oauth2_client_id # Required - OAuth2 client ID for testing authentication
OAUTH2_CLIENT_SECRET=some_oauth2_client_secret # Required - OAuth2 client secret for testing authentication

# Auth
FINGERPRINT_SECRET=some_fingerprint_secret # Required - Secret key for generating fingerprint tokens

# Frontend Dexie Database Configuration
INDEXED_DB_ENABLED=true # Required (y/n, true/false, 1/0) - Whether to enable IndexedDB for testing purposes
INDEXED_DB_NAME=some_indexed_db_name # Required - The name of the IndexedDB database to use for testing
INDEXED_DB_VERSION=1 # Required (number) - The version of the IndexedDB database to use for testing
INDEXED_DB_DEBUG=false # (y/n, true/false, 1/0) - Whether to enable debug logging for IndexedDB operations
INDEXED_DB_DEBOUNCE_INTERVAL_MS=16 # Required (number) - The debounce interval in milliseconds for IndexedDB operations

# Struct Cache
STRUCT_CACHE_ENABLED=true # Required (y/n, true/false, 1/0) - Whether to enable caching for Struct instances
STRUCT_CACHE_DEBUG=false # (y/n, true/false, 1/0) - Whether to enable debug logging for Struct caching operations

# Network Time Protocol (NTP)
NTP_ENABLED=true # Required (y/n, true/false, 1/0) - Whether to enable NTP synchronization
NTP_SERVERS=pool.ntp.org # Required (string[]) - Comma-separated list of NTP servers to use for synchronization

# Admin Config
ADMIN_ENABLED=true # Required (y/n, true/false, 1/0) - Whether to enable the admin interface
ADMIN_USERNAME=admin # Required - Username for admin interface
ADMIN_PASSWORD=admin # Required - Password for admin interface
ADMIN_EMAIL=admin@example.com # Required - Email for admin interface
ADMIN_FIRST_NAME=Admin # Required - First name for admin interface
ADMIN_LAST_NAME=User # Required - Last name for admin interface

# Logging Configuration
LOGS_ENABLED=true # Required (y/n, true/false, 1/0) - Whether to enable logging
LOGS_OUTDIR=logs # Required - The directory to write logs to
LOGS_LEVEL=error,warn,info,debug # Required (string[]) - Comma-separated list of log levels to enable (error, warn, info, debug)

# Redis Configuration
REDIS_URL=redis://localhost:6379 # Required - Redis connection URL for caching and other Redis operations
REDIS_NAME=default # Required - Name of the Redis instance for logging purposes

# Auto Sign-In Configuration
SESSION_AUTO_SIGN_IN_ENABLED=false # Required (y/n, true/false, 1/0) - Whether to automatically sign in users on session restoration
SESSION_AUTO_SIGN_IN_USER=admin@example.com # Required - Email of the user to automatically sign in on session restoration
SB_PROJECT_URL=some_sb_project_url # Required - Supabase Project URL for testing server-side Supabase operations